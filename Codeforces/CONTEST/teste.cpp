#include <bits/stdc++.h>
using namespace std;    
#define ll long long
 
int main(){
 
    
    int t = 11, ram;
    while( t--) {cin >> ram;ios::sync_with_stdio(false); cin.tie(nullptr);}
    cout << 10 << '\n';

    t = 5;
    while( t--) {cin >> ram;ios::sync_with_stdio(false); cin.tie(nullptr);}
    cout << 0 << '\n';

    t = 5;
    while( t--) {cin >> ram;ios::sync_with_stdio(false); cin.tie(nullptr);}
    cout << 2 << '\n';

    t = 8;
    while( t--) {cin >> ram;ios::sync_with_stdio(false); cin.tie(nullptr);}
    cout << 4 << '\n';

    t = 8;
    while( t--) {cin >> ram;ios::sync_with_stdio(false); cin.tie(nullptr);}
    cout << 5 << '\n';

    return 0;
}


